{
  "name": "hive",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "hive": "src/cli.ts"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test && bun run test:sessiond",
    "test:sessiond": "native/sessiond/test.sh",
    "dev": "bun run src/cli.ts",
    "build:release": "bun run src/release/build.ts",
    "release:plan": "bun run src/release/plan-cli.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^15.0.0",
    "fastembed": "^2.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
