{
  "name": "hive",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "hive": "src/cli.ts"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "check": "bun run lint && bun run format:check && bun run typecheck",
    "lint": "biome lint . --error-on-warnings",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "typecheck": "tsc --noEmit",
    "test": "bun run scripts/test-sandbox.ts -- bun test || { code=$?; echo 'TEST SUITE INCOMPLETE: sessiond leg DID NOT RUN because the Bun leg failed.' >&2; exit $code; }; bun run test:sessiond",
    "test:sandbox:self-test": "bun run scripts/test-sandbox.ts --self-test",
    "test:live": "HIVE_ACP_LIVE=1 HIVE_CLAUDE_LIVE=1 HIVE_CODEX_LIVE=1 HIVE_DURABLE_RESUME_LIVE=1 HIVE_USAGE_PARITY_LIVE=1 HIVE_LIVE_CAPABILITIES=1 HIVE_LIVE_MEMORY_EMBEDDINGS=1 bun run scripts/test-sandbox.ts -- bun run scripts/test-live.ts",
    "test:sessiond": "native/sessiond/test.sh",
    "dev": "bun run src/cli.ts",
    "build:release": "bun run src/release/build.ts",
    "release:plan": "bun run scripts/release/plan-cli.ts",
    "cutover:acceptance": "bun run scripts/release/acceptance-cli.ts"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.3.0",
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@opentui/core": "^0.5.0",
    "commander": "^15.0.0",
    "diff": "^9.0.0",
    "fastembed": "^2.1.0",
    "onetime": "^8.0.0",
    "p-retry": "^8.0.0",
    "web-tree-sitter": "0.25.10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
