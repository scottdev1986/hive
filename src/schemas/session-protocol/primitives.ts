import { CAPABILITY_PROVIDERS, CapabilityProviderSchema } from "../provider";

export * from "../primitives";

export const SESSION_PROTOCOL_PROVIDERS = CAPABILITY_PROVIDERS;
export const SessionProtocolProviderSchema = CapabilityProviderSchema;
