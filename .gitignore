# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns

# --- IDEs & editors ---
.idea/
*.iml
.vscode/
!.vscode/extensions.json
.fleet/
.zed/
*.swp
*.swo
*~

# --- Node / Bun / TypeScript ---
node_modules/
dist/
build/
out/
*.tsbuildinfo
# `bun build --compile` scratch files, written beside the project root
.*.bun-build
coverage/
.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# --- Environment & secrets ---
.env
.env.*
!.env.example

# --- hive runtime (repo-local state that must not be committed) ---
# All of .hive/ is local working state now — agent worktrees, runtime state,
# and the memory/skills that earlier Hives committed. It stays on disk; git
# just stops tracking it.
.hive/

#Coding Agents
.claude/
.codex/
# hive writes per-worktree MCP wiring at spawn (see writeClaudeAgentConfig)
.mcp.json
# hive symlinks shared skills into each worktree at spawn (see
# src/adapters/skills.ts); not anchored to .hive/worktrees/ because this
# file is evaluated from each worktree's own root, where that path never
# matches.
.agents/

# --- local working docs (kept on disk, out of git) ---
docs/
research/
prototypes/

apple
