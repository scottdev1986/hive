Slice 2 review at d216841: NEEDS FIX emerging. (1) Terminal race remains: recovery rechecks terminal only at recovery.ts:339-359, then awaits hasSession and many operations before newSession at 561; hive_kill can mark dead after that check, teardown the session, and recovery upserts stale live row at 433 then relaunches. Added test marks dead during the earlier sweep probe, so it cannot cover this window. (2) reap rollback at server.ts:2079-2085 restores the pre-terminal live row when teardown/readback fails; if tmux was killed but ps verification failed, the next sweep can relaunch an explicitly killed agent. (3) resume server.ts:1510-1535 issues credential + persists idle/unrevoked before SIGCONT, swallows SIGCONT failure, returns resumed:true. Crash/failure leaves valid authority and truthful state broken, with a stopped process shown idle. Continuing app-server/PID/stale-session/test audit.
