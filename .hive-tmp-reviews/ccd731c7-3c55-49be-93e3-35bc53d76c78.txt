Codex review slice 2/3 complete — VERDICT: NEEDS FIX at d216841 vs 0681f8a.

CRITICAL 1 — terminal is not authority-revoking, leaving a durable crash window. server.ts:2065-2069 marks dead, but revoke/removeCredential is delayed to stopAgentProcesses callback at 2075-2078; teardown.ts:330-335 awaits capture before callback. db.ts:1029-1045 markAgentDead changes status only. Capability authority returns terminal rows' unchanged epoch/writeRevoked (server.ts:670-675), checks only those (capabilities.ts:340-373), and landAgent has no terminal-status refusal (server.ts:2504-2540). Crash after dead commit and before callback leaves live process + valid token able to mutate/land indefinitely after daemon restart.

CRITICAL 2 — explicit kill can still race automatic recovery into relaunch. recovery.ts:339-359 fresh-checks terminal, then awaits hasSession at 363 and many more operations; resume writes stale live record at 433-438 and newSession at 561-565 without another terminal CAS/check. Interleaving: recovery passes fresh check; kill marks dead and tears session down; recovery sees no session, overwrites dead/closed row with stale agent, launches replacement. recovery.test.ts:240-268 is false-green: it marks dead during the earlier sweep hasSession probe, before the fresh check, not after it.

HIGH 3 — teardown failure explicitly undoes durable terminal intent. server.ts:2079-2085 restores the original live row. If tmux kill succeeded but ps verification failed, session is absent and next recovery sweep may relaunch the deliberately killed agent. It also restores writeRevoked=false after the callback may already have revoked/removed credentials, making DB truth disagree with actual authority.

HIGH 4 — resume grants authority and claims success before proving SIGCONT. server.ts:1510-1528 mints/writes the new credential and persists idle/writeRevoked=false before resume; 1529-1535 swallows any error and 1536-1540 returns resumed:true. Crash/failure leaves a stopped or absent process shown idle with authority restored; hive_resume then refuses because it is no longer paused. Worse, teardown.ts:308-313 treats missing tmux as roots=[], and resumeTmuxSession 279-291 no-ops successfully, so a vanished process gets “same process resumed.” server.test.ts:807-864 only uses an always-successful resume mock.

HIGH 5 — pause/resume does not prove the same complete provider process tree. teardown.ts:7-9 states Codex app-server host is a daemon child unreachable from pane roots. stopAgentSession includes host PID (374-385), but server.ts:840-851 pause/resume call tmux-only helpers with no host root; only a best-effort native interrupt precedes pause (1368-1381). Thus app-server host is never SIGSTOP-verified. Separately CapturedTree stores only pid+command (teardown.ts:45-47,124-126); resume discards the pause capture and re-captures current tmux PIDs (276-291), with no process birth/start identity or toolSession binding. PID reuse/stale pane can SIGCONT an unrelated/replacement tree. teardown.test.ts:99-129 resumes the same in-memory captured fake and cannot exercise either defect.

MEDIUM — daemon-applied pause can overclaim preservation. sessionRoots returns [] for missing session (teardown.ts:308-313), suspend then reports empty success; processes disappearing between capture/signal are counted suspended (203-239). delivery.ts:717-729 marks applied after this, even though no same process/context survives. The pause test at server.test.ts:758-805 injects a successful fake and asserts only final state, not real roots/host/identity.

Verified positives: critical send transactionally calls db.revokeAgentCapabilities before delivery (delivery.ts:288-314; db.ts:1379-1392); successful pause checks unstopped before applied (server.ts:1395-1413; delivery.ts:717-729); paused row/control/epoch are durable DB fields and normal crash recovery skips live tmux / refuses revoked paused recovery (recovery.ts:217-262,289-305); stop/cancel/restrict-writes remain destructive through server.ts:728-753. These do not close the failures above.

Checks: git diff --check passed. Exact-target tests/typecheck were not runnable without materializing/checking out d216841; this agent is capability-enforced read-only and its worktree remains at baseline 0681f8a. No files modified.
