Additional HIGH/CRITICAL integration omission: the native Codex app-server writer path has no mutation guard at all. `buildCodexAppServerCommand` at src/adapters/tools/codex-app-server.ts:731-759 launches `codex app-server` with apps/MCP/trust only—no `features.hooks`, no `hooks.PreToolUse`, no multi_agent disable. `startAgent` at :372-431 starts workspace-write/on-request threads. The generated project config intentionally contains no hooks (src/adapters/tools/codex.ts:522-532), and branch comments say project-local config is not loaded under override-only trust. Spawner selects this native path for configured app-server writers (src/daemon/spawner-impl.ts around 2040-2130). Thus app-server `apply_patch`/workspace writes bypass the new guard entirely; its approval handler also does no identity attestation. Existing app-server diff/tests only retain effort, not guard coverage.
