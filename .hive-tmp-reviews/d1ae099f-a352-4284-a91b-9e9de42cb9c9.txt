Route-audit contract gaps (MEDIUM, plus the already-sent routing bypass): a39038c claims one audit per routing decision, both selection/refusal, with original chain/per-link results. Actual insert exists only in non-explicit policy branch at src/daemon/spawner-impl.ts:1804-1821. Explicit-model decisions (:1655-1682), never-configured policy (:1697-1701), and empty-chain refusal (:1787-1792) produce no audit. Successful eligible links are never appended to `attempts` (:1703-1739), and QuotaService reports independence exclusion only when filtering leaves zero candidates (src/daemon/quota.ts:1537-1553), so a successful cross-provider review typically persists `attempts: []`: neither original chain nor excluded same-provider candidate is explainable. Tests assert only Array.isArray, not content/bounds. DB storage itself is prompt-free and globally bounded to 500 with synchronous insert+prune (src/daemon/db.ts:1661-1705); agent scoping is correct, and no direct secret/prompt field is stored.
