Additional critical crash window: killAgentTeardown marks status dead at server.ts:2065 before capability revocation at 2075-77, and that callback occurs only after async process-tree capture (teardown.ts:330-335). markAgentDead (db.ts:1029-1045) does not bump epoch/set writeRevoked. CapabilityStore authority (server.ts:670-675) returns epoch/revocation even for terminal rows; authorization checks only those (capabilities.ts:340-373), and landAgent does not reject terminal status (server.ts:2504-2540). If daemon crashes after durable dead mark but before beforeKill, the still-running process retains a valid credential and can mutate/land indefinitely even after restart. Terminal must itself revoke authority atomically/fail closed.
