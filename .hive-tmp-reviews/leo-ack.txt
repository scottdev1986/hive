Acked all 3 FINAL reviews (Lena/Noah/Priya) + route-audit + status/docs findings — ~20 items. Starting the fix round now on this branch; NOT landing; will not declare a new HEAD until I've addressed all critical/high + named-medium and re-run full tests. Committing each fix separately.

Architecture synthesis (the findings converge here): per-mutation identity enforcement is NOT achievable on Codex 0.144.4 — TUI PreToolUse hooks are fail-open (failure/timeout/invalid-JSON => should_block=false) AND writer-tamperable (P1/P3); app-server has no hooks/attestation at all (P5/L4). So:
- REMOVE the fail-open/tamperable PreToolUse guard hook subsystem entirely; stop claiming per-mutation writer safety from it.
- REFUSE Codex app-server WRITER launches (preserve app-server read-only/root/query; NO silent fallback to an unenforced TUI writer); disable multi_agent on app-server argv even read-only (L5).
- Rest TUI-writer safety on FAIL-CLOSED, tamper-resistant, daemon-side boundaries only: kernel sandbox (worktree containment) + the 30s identity SWEEP (reads the rollout directly => drift => SIGSTOP) + a FRESH-reattest landing gate (P2/L2). Document the limitation + bounded blast radius truthfully (own sandboxed worktree, <=one sweep, never mergeable).

ONE product decision I want your call on (I'll default to the above unless you say otherwise): do you want the STRONGER containment of refusing ALL Codex WRITER launches (TUI too) until an enforceable broker/sandbox boundary exists, or is the daemon-side containment above (app-server writers refused, TUI writers contained) acceptable? The former is a ~1-line authorization refusal on top of my plan; the latter keeps Codex writers working with honest bounded safety.

Fix order: (1) terminal-state fail-closed at all layers incl. atomic revoke+epoch+writeRevoked on terminal, auth/landing reject terminal regardless of epoch, revoke-before-teardown (Noah CRITICAL); recovery late terminal check + reap preserves terminal truth (N1/N2). (2) app-server writer refusal + hook removal + fresh landing gate (architecture). (3) resume SIGCONT-first fail-closed (N3). (4) pause binds exact captured tree incl app-server host, empty/incomplete capture fails pause (N5). (5) telemetry newest-incomplete=unknown (L1). (6) recovery attestation reset on new incarnation (L3). (7) cli/status launch-vs-observed (L6). (8) spawner-path review exclusion (P4). (9) route-audit centralization one-per-decision (P6). (10) docs correction + pre-column migration regression (L7/L8) + residual note (L9). Proceeding on (1) now.
