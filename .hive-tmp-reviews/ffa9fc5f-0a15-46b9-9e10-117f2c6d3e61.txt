FINAL review slice 1/3 — d2168411944fbb4733f15b0fad3dfdfe85f0db41 vs 0681f8adf4a59060879c5ab13bc6ba7a9888aa23: NEEDS FIX.

P1 HIGH — stale rollout evidence can be promoted to matching. tool-telemetry.ts:259-275 scans backward and, when the newest in-cwd turn_context is incomplete (missing model/effort) or its partially-written JSON was skipped, continues to an older complete record. Failure: prior turn Sol/xhigh matches; current turn_context is incomplete/unparseable; reader returns prior Sol/xhigh as a fresh observation instead of unknown, authorizing the current turn. tool-telemetry.test.ts:294-312 tests an incomplete record alone, not incomplete-newest + older-valid.

P1 HIGH — the “per-mutation” guard trusts stale durable state. server.ts:2589-2591 returns allow immediately for any stored matching row and reads provider evidence only at 2601-2604 for nonmatching rows; server.test.ts:3872-3874 explicitly locks this fast path. At turn-start, unknown/absent is intentionally not persisted (3654-3687 only handles drift). Failure: previous turn matched, model/effort changes, new turn observation is not yet readable; row remains matching and first mutation is allowed before sweep. This violates “before any writer may mutate.”

P1 HIGH — crash recovery reuses predecessor attestation. recovery.ts:433-438 and 582-586 relaunch a new Codex process while preserving observedIdentity/identityState. A recovered row that was matching enters idle still matching, then the guard fast path authorizes without observing the relaunched process. Reset to unknown/unattested and require fresh exact-session/turn evidence.

P1 HIGH — app-server driver bypasses the new identity contract. codex-app-server.ts:393-416 sends requested model/effort, emits session-start without thread id/observed identity, and no production caller uses source “codex-app-server” (only identity-attestation.test.ts:76-85). Host argv at 742-759 enables neither hooks nor the PreToolUse guard; thread is workspace-write. turn-start notification at 563-570 also carries no identity. App-server approvals are resolved at server.ts:4207-4224 without an identity gate. Thus opt-in app-server writers can mutate while permanently unattested, and requested launch fields are the only model fed to quota at codex-app-server.ts:543-554.

P1 HIGH — internal Codex subagents remain enabled for app-server. TUI spawn/resume correctly add features.multi_agent=false at codex.ts:124-141, but exhaustive app-server argv codex-app-server.ts:742-759 omits it. The adapter test at codex-app-server.test.ts:98-114 checks apps/MCP scoping but not multi_agent. docs/providers/launch-mechanics.md:105 falsely claims every spawn/resume disables it and claims SubagentStart/Stop backstops; there are no such hooks in src.

P2 MEDIUM — human CLI status still conflates launch and observation. cli/status.ts:18-23 labels MODEL as running and renders liveModel ?? model, so an unobserved Codex row displays the launch request as runtime fact; status.test.ts:8-41 enshrines it. cli/control.ts:186 is the public caller. compact hive_status is correct at orchestrator-lifecycle.ts:184-196 (separate model/liveModel/liveEffort/state), but “hive status” is not.

Verified positives: exact rollout lookup binds both toolSessionId and session_meta cwd (codex.ts:304-325,381-387); child record with different cwd is ignored; liveness promotion does not set attestation; nullable additive DB migration leaves legacy rows unattested (db.ts:621-642), centralized rebuild retains columns; app-server follow-up effort persists (codex-app-server.ts:408,424-435); app-server→TUI fallback resets unknown and clears observation (spawner-impl.ts:2147-2157); MCP compact status separates launch/observed.

Verification: git diff --check passed. Focused target tests/typecheck could not run: this capability-enforced read-only worktree is checked out at the base commit, and executing d216841 would require materializing/changing a tree. No files modified. Residual: no real-provider acceptance test proves Codex hook session-id capture, and no migration test opens a genuinely pre-column DB for these three new fields.
