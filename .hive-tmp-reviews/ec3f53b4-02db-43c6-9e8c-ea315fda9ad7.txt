Landing gate is also stale-attestation vulnerable (HIGH): src/daemon/server.ts:2532-2547 checks only persisted `identityState`, never reattests at land time. At turn-start, :3654-3687 acts only on confirmed drift; an early `unknown` does not overwrite an older `matching`. Combined with app-server's missing PreToolUse guard (and TUI guard's matching fast path), a previously matching writer can begin a drifted/temporarily unreadable turn, mutate, and land before the 30s sweep while the row still says matching. The new landing test only writes `identityState: matching`; it does not inject a live drift reader. A truly current matching attestation must be reread immediately in the landing choke point and fail closed on unknown/error before invoking git.
