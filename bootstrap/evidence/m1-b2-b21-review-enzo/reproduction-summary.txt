M1-B2.1 cross-vendor review (enzo/Claude) — reproduction summary
Pin: 2b42c449  Author: elaine (Codex)  Verdict: NO-LAND

HOW TO REPRODUCE THE BLOCKING FINDING
  1. git worktree add --detach <scratch> 2b42c449
  2. cd <scratch> && bun install --frozen-lockfile
  3. cp orphaned-lease-reproducer.test.ts.txt \
       <scratch>/test/terminal-host-freeze/probe-edge3.test.ts
  4. bun test test/terminal-host-freeze/probe-edge3.test.ts

OBSERVED
  A: inc-1 B: inc-2
  leases held: A true | B true
  advance() THREW: StaleIncarnationError stale incarnation for k3
  A lease still held after throw: true
  B lease still held after throw: true

WHY IT BLOCKS
  Same-source second create over a still-active lease is admitted
  (neutral-visibility-fixture.ts:126-128 compares only against a DIFFERENT source).
  The base host supersedes the record; the prior incarnation goes stale while its
  lease is still held. At expiry, expireLease -> terminal.terminate() throws on the
  stale incarnation, and advance() (lines 201-203) has no per-lease error isolation,
  so the throw escapes and skips teardown of EVERY remaining lease.
  Net: both generations outlive their deadline, un-torn-down, with no typed failure
  state and no expiry evidence. This is the §4 "bounded expiry + verified teardown"
  guarantee that B2.1-B2.6 inherit.

NON-BLOCKING (see full record)
  O1 qualifier line 252 `if rg` fails open (rg absent to bash); claim hand-verified true
  O2 replayed create can assert an active lease for a torn-down session (fails closed)
  O3 rejections cached under the idempotency key; corrected retry replays stale rejection

SUITES REPRODUCED INDEPENDENTLY AT THE PIN (not from the author's /tmp artifact)
  bun test ................ 1712 pass / 10 skip / 0 fail   (real exit 0)
  bun run typecheck ....... clean                          (real exit 0)
  terminal-host-freeze .... 26 pass / 0 fail
  swift test (full) ....... 321 executed / 2 skipped / 0 failures (real exit 0)
  dual-arch B2 qualifier .. exit 0, arm64 + x86_64
  suppression suite ....... answered_when_enabled_count=7, vacuous_count=0, BOTH arches
  bare-Ghostty public symbols ... 0 on both arches (394-line non-vacuous dump)

MUTATION CONTROLS: all eight L-S bite at the intended assertion (verified by capturing
each faulted failure diff, not merely that the case threw).
