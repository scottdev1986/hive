/// HiveTerminalKit — AppKit host for a Ghostty-owned terminal. Production panes use stock `ghostty_surface_new` with a command; Ghostty owns the PTY and child. Swift forwards size, focus, and input.
