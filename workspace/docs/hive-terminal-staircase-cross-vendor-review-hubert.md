# Live terminal OPOST/staircase fix — cross-vendor review (hubert, Codex)

Reviewing hattie (Grok): original pin
`47e1777c6aa9d2cbc9f1489a62046e6f10e45832`, plus evidence clarification
`5cb663b042b525a2937af510ae9d71fa9192efa1`, on
`hive/hattie-continue-a-crashed-agent-s-wor`.

**VERDICT: PASS — clear to land.** The fix, kernel-boundary regression test,
suites, committed visual evidence, digest regeneration, and residual-marker
disclosure all pass review. The exact independent live recipe found a separate
attach/replay problem: its fresh Workspace viewer remained blank even though
the session journal contained CRLF `ls -1` output. The orchestrator accepted
that as a separately routed defect and queued live re-verification; it is not
caused by, and does not block, this termios fix.

## Source audit

- `applyTerminalProfile` calls `cfmakeraw`, then unconditionally restores
  `c.OPOST | c.ONLCR` on `c_oflag`. The profile-controlled input flags remain
  selectively enabled after that assignment.
- `profileMatches` independently requires both `OPOST` and `ONLCR`, so drift in
  either flag fails readback.
- `TerminalProfile` gained no output-translation field. The rejected ghost
  branch's opt-out knob is absent; the output contract is always on.

## Blind-spot mutation

I temporarily removed both coupled fix portions (the `c_oflag` restoration and
the two `profileMatches` requirements), leaving the new test unchanged. This
recreated the pre-fix behavior without failing earlier in profile readback.
The unpiped native run returned `1` and the named test failed exactly:

```text
expected: a\r\nb\r\n
found:    a\nb\n
First difference: expected 0x0d, found 0x0a
Build Summary: 49/53 steps succeeded; 196/198 tests passed; 2 failed
```

The other failures were downstream CRLF consumers (`neutral_host` attach and
`session_host` graceful-action echo), consistent with the same reverted
contract. I restored `native/sessiond/src/pty_host.zig` byte-for-byte; its
SHA-256 returned to
`2550de1a492b21f5eb5123b50a06708508f99629ed353cbcdaed471ff8571865`.

## Green checks (real exit codes, no suite pipes)

| Check | Result |
|---|---|
| direct `zig build test --summary all` | exit 0; 47/47 steps, 198/198 tests |
| `HIVE_NATIVE_CACHE=… bun run test:sessiond` | exit 0; Zig/native harness plus TypeScript identity/live-create checks |
| `scripts/check-ghostty-abi.sh` | exit 0; C and Zig ABI checks for arm64 and x86_64 |
| `swift test --filter LiveHostAttachTests` | exit 0; compiled successfully, 4/4 opt-in tests honestly skipped without `HIVE_B22_PROOF_HOME` |
| `bun run typecheck` | exit 0 |

The two standalone-header sites were both exercised: the syntax-only compile in
`native/sessiond/test.sh` and the dual-architecture compile in
`scripts/check-ghostty-abi.sh`.

The brief's phrase “Swift-side attach assertions now expect CRLF” is not true
of the tree. The amended evidence says so explicitly. The three CRLF assertions
are Zig-side: the primary `pty_host` bare-NL test, `session_host` PTY echo, and
`neutral_host` attach payload. The Swift live tests cover response/geometry and
were the prior blind spot; the mutation-proved `pty_host` test closes it at the
actual termios boundary.

## Digest fixtures

The 1 MiB and 100 MiB fixtures were regenerated by changing their deterministic
byte generators: generated `0x0A` is mapped to `0x80` before the data traverses
the slave PTY. No expected digest or golden hash was edited to bless transformed
output. This is principled fixture generation for an output-processing PTY,
not expectation laundering.

## Visual evidence and residual

The committed BEFORE frames genuinely show the defect: `ls` rows continue at
the previous row's column, and zsh's inverse-video `%` partial-line marker is
misplaced onto normal output. The committed AFTER frame shows one aligned entry
per row and no `%` box on any `ls` output row. It also retains one `%` after a
genuinely partial/login-handoff line; that is not hidden.

`raw/hattie-live-terminal-staircase-fix.md` now states the exact boundary:
complete lines are fixed, while residual `PROMPT_EOL_MARK` on genuinely partial
output remains and is a C1 decision. It also records the rejection of the
unrequested `output_translation` profile knob.

## Independent live rerun and separate finding

Fresh `make terminal` attempts used an unlocked Aqua session, short
`/tmp/hb22-*` runtime paths, and exclusive ownership of port 43117. The real
shell did execute `ls -1`; `journal.bin` contains `Makefile` and the other
entries, with the output traversing the fixed PTY. The Workspace terminal pane
nevertheless remained blank. Its stderr repeatedly reported:

```text
workspace visibility publish failed: HTTP 409
```

That 409 is normal for this harness: its publisher owns and renews the visibility
lease while Workspace's competing publish is refused. To eliminate contamination
from exploratory probes, I then reproduced Hattie's known-good recipe verbatim
in a completely fresh home, with no pre-attach grant, secondary viewer, click,
or typed input. The exact `/tmp/hbt-ls-shell` wrapper ran `/bin/ls -1`, printed
the handoff line, and `exec`'d `/bin/zsh -l`; `make terminal` launched the only
Workspace viewer. Positive controls all passed:

- auto home `/tmp/hb22-0fee` and sole port-43117 stack;
- harness log `interactive login shell: /tmp/hbt-ls-shell -l`;
- matching Workspace `--instance-home` and production viewer;
- visibility renewals every five seconds; and
- journal bytes `assets\r\nbootstrap\r\n…`.

After more than 30 seconds the green-bordered terminal pane was still blank.
This reproduces a separate attach/replay/geometry failure on the current
combined tree, not an OPOST failure: the authoritative journal proves that the
real PTY emitted the required CRLF. No blank capture is committed as positive
evidence. The transcript and runtime evidence were reported to the orchestrator
for a separate defect and a queued live re-verification.

All temporary Swift and harness probes were restored byte-for-byte. After the
final controlled SIGINT teardown, all proof PIDs were gone and
`lsof -nP -iTCP:43117 -sTCP:LISTEN` returned `1` (no listener). Landing is
clear for the termios fix; the committed Hattie BEFORE/AFTER pair itself is
genuine, and the failed reviewer render remains explicitly separate rather than
being misreported as a successful fresh screenshot.
