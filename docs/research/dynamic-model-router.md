# Dynamic model routing: capability-aware, effort-first, release-independent

## What this is and why

This is a design draft, research only — nothing here is implemented. It answers one question: what has to change for Hive's router to assign the best model *and* effort level per task, without a Hive release standing between the user and a model that shipped yesterday.

Today's router is a shipped table with a user override file. That shape has three structural gaps, and each is a required property of the overhaul:

1. **Capability-awareness.** Nothing in the resolver knows what a model *is*. The tier table encodes capability implicitly (`cheap` names `haiku`), but the moment routing becomes dynamic — quota valves, downshift chains, derived defaults — there is no floor stopping a coding task from landing on a Haiku-class model. The gate that refuses that spawn does not exist.
2. **Effort as a first-class knob.** `hive_spawn` takes a `model` but no `effort` (`src/daemon/spawner.ts:8-19`). The Codex route column carries effort; the Claude column does not (`src/schemas/routing.ts:14-29`), so every Claude agent Hive spawns — including cheap-tier ones — reasons at the CLI's `high` default. Effort is half a knob, on one vendor, reachable only through the table.
3. **Release-independence.** The router's knowledge of the model landscape is compiled in: `CLAUDE_BEST_MODEL` and `CLAUDE_OPUS_MODEL` are constants verified by hand against an installed CLI (`src/adapters/tools/models.ts:9,19`), `FABLE_AUTO_ROUTING_CUTOFF` is a date baked into `schemas/routing.ts:77`, the Fable→Opus release valve is an `if (claudeModel === CLAUDE_BEST_MODEL)` splice in the spawner (`src/daemon/spawner-impl.ts:1061-1063`), and `modelVendor` recognizes vendors by regex and a hardcoded alias list (`models.ts:27-39`). Every one of these goes stale the day a lineup turns over, and lineups turned over twice last year. Users should never wait on a Hive release to route to a new model; shipped tables must become the fallback, not the truth.

The thesis: **keep the split SPEC §6 got right — the orchestrator classifies, a table resolves — but stop shipping the table's contents and start deriving them.** The orchestrator's judgment ("this is deep work") is durable. The table's *structure* (tier × tool, override-surviving routes) is durable. The table's *values* — which concrete model is the account's best, whether it takes an effort flag, what to prefer when the first choice is exhausted — are the intersection of two things Hive can obtain at runtime: facts both vendors already publish (the account's models and their effort levels), and curated judgment that must be fetched, not compiled in. Hive already has the machinery pattern for the first half: quota discovery replaced `quota.toml` transcription with provider-read pools, denominated in what the provider actually reports, with shipped behavior as compatibility fallback (SPEC §6). This design applies the same move to capability, and takes the second half — the curated routing manifest SPEC §6 already promises — seriously enough to specify what trusting it requires.

What this doc is not: a vendor-surface catalog (that is `docs/research/router-vendor-surfaces.md`, whose findings this design builds on) and not a cost/tier rubric (that is `docs/research/model-routing-and-token-efficiency.md`, whose R2/R3/R4 this design generalizes).

## Where the router stands today, in one paragraph

The orchestrator classifies a task into one of four tiers; `resolveRoute` merges `~/.hive/routing.toml` over `defaultRoutingTable(now)` per tier and per tool, overrides always winning (`src/config/load.ts:70-119`). The spawner resolves both vendors' concrete models — `best` through a shipped constant, `default` by reading the tool's own user config (`src/adapters/tools/models.ts:79-94`) — splices in Opus 4.8 after Fable as a release valve, and hands the candidate list to the quota resolver, which scores by worst post-reservation headroom, filters by viability, and reserves atomically (`src/daemon/spawner-impl.ts:1036-1079`, SPEC §6). The winning concrete choice is frozen into an `ExecutionIdentity` — `{tool, model}` for Claude, `{tool, model, effort}` for Codex (`src/schemas/agent.ts:28-38`) — which a critical-control restart replays verbatim and fail-closed (`spawner-impl.ts:573-594`). An explicit `hive_spawn` model is a user directive: vendor-bound, launched verbatim, never substituted. All of that machinery is sound and survives this design; what changes is where its inputs come from.

## The capability record: what discovery can tell us

The design's first new object is a per-model **capability record** of discovered facts — read from the provider at runtime, zero-cost, provenance-stamped exactly as quota pools are. Verified live on this machine, 2026-07-11, claude 2.1.207: the stream-json `initialize` control request Hive already awaits returns a `models` array in which every entry carries `value`, `resolvedModel`, `displayName`, `supportsEffort`, `supportedEffortLevels`, and `supportsAdaptiveThinking`, plus an `account` block with `subscriptionType`. Concretely, this account enumerates `default` → `claude-opus-4-8[1m]`, `opus[1m]`, `claude-fable-5[1m]`, `sonnet` → `claude-sonnet-5`, `haiku` → `claude-haiku-4-5-20251001` — and Haiku's entry *omits* the effort fields entirely. Absent, not `false`: as the vendor-surfaces doc puts it, omission may mean unsupported, rollout-gated, or simply missing from this protocol version, and the record must preserve that three-way distinction rather than promote it to a boolean. On the Codex side the app-server's `model/list` is a full peer, and officially documented: zero-cost, returning *account-visible* models — entitlement included — with per-model default and supported reasoning efforts, hidden/default flags, input modalities, and service tiers (verified by caleb on codex 0.144.1). `config/read` separately reports the effective model and effort an unflagged launch would use. Discovery is a daemon-side act, so record freshness does not depend on which driver later spawns the agent.

Three rules govern reading these surfaces, all inherited from the vendor-surfaces doc. **Presence is positive evidence; absence is unknown** — Claude's menu omits aliases that launch fine (`best`, bare `claude-opus-4-8`), so no absence anywhere is treated as proof of impossibility. **Vendor-hidden entries are not Hive's to route** — an entry flagged hidden (Codex's `codex-auto-review`) is excluded from automatic selection even if a stale manifest still lists it, and stays reachable by explicit pin; when the vendor and the curator disagree about whether a model is internal, the vendor is fresher. **A model name is three facts, not one** — the record keeps the *launch token* (what `--model` receives), the *canonical id* (the join key for quota pools and the manifest), and the *variant* (`[1m]` names a context-window entitlement the CLI appends and the launch flag must never receive). The first draft treated stripping `[1m]` as hygiene; review correctly objected that the variant is an entitlement fact worth keeping, so it is stored, just never launched.

The record, keyed by **(provider, account, canonicalId)** — account in the key because entitlement and the alias menu are facts about a signed-in account, not about a model: `{launchToken, variant?, aliases[], entitled, hidden, effortLevels?, defaultEffort?, observedAt, cliVersion}`, carrying the CLI version that produced it (a catalog read from claude 2.1.207 is not a claim about 2.2's). Records refresh at daemon start and on the maintenance tick; both surfaces are zero-cost and one is already awaited. A record older than a configured freshness TTL (defaulting to one maintenance-tick interval plus slack) no longer supports *derivation* — a spawn consulting it falls down the fallback ladder below — but still supports *validation*, with staleness named in the warning. Cached in SQLite next to the quota pools it will be consulted beside.

What discovery cannot produce is judgment: nothing in either catalog says which model should carry a coding tier. That is the manifest's job.

## The manifest: ordered candidates, and the trust they require

The first draft carried judgment as a class ontology — every model assigned `frontier|mid|cheap` plus a rank within its class, tiers targeting a class and flooring at another. Cross-vendor review proposed the simpler shape, and the simpler shape wins: **the manifest publishes, per tier × vendor, an ordered list of candidate canonical ids, plus one per-model flag: `codingCapable`.** Derivation intersects the list with live discovery — entitled, non-hidden, fresh — and takes the first survivor as the primary; the *remainder of the list* is the downshift chain; the *end of the list* is the floor. There is no class to assign, no rank to reconcile, no target-class and floor-class pair per tier.

The class ontology is retired as the rejected alternative, and honestly: it lost because its load-bearing content had already leaked out. Once review forced `rankInClass` into the manifest (class alone cannot choose between an account's two frontier models), the ordering was doing all the routing work, and class had shrunk to a grouping label for orderings — two indirections (model→class, tier→class) where one list encodes the answer directly. Class boundaries are also the most contestable judgment in the system (is Sonnet 5 "near-Opus" mid or frontier? the vendor's own positioning argues both), whereas "what should a standard-tier Claude spawn try, in order" is precisely the question the curator is answering anyway. What class genuinely bought — a vocabulary for describing *new* tiers or user-defined profiles without enumerating models — is real but speculative, and speculative flexibility is exactly what this repo's guidelines say not to build. The `codingCapable` flag survives because it is not ontology, it is the floor fact itself, consumed by the `kind` gate below. Manifest authors may keep class labels as commentary; routing never reads them.

A manifest this load-bearing cannot be an unspecified fetch, and review was right to make its trust pipeline a blocker rather than a footnote. The requirements, named: an **owner** (Hive's maintainers publish it; publishing is decoupled from binary releases — that decoupling is the release-independence claim, and if in practice manifests only ship with releases, the claim is dead and this section is where to admit it); **signing** (detached signature over the manifest, verified against a key shipped in the binary — a routing table is an instruction about where code executes, and an unsigned fetched one is an injection vector); **schema versioning** (unknown fields preserved verbatim so old Hives pass through what newer manifests say; unknown *major* version rejected); **expiry** (a `validUntil` past which the manifest still validates but no longer drives derivation — an expired judgment degrades like a stale record, loudly, rather than governing silently forever); **atomic install with rollback** (the previous manifest is kept; a failed verify or parse changes nothing); a **kill switch** (one config flag reverts routing to the shipped table, because the day the manifest is wrong is the day the user has no patience for editing pins per tier); and an **audit trail** (every routing decision's `reason` records the manifest version it consulted, so a bad route is attributable to the judgment that produced it). The migration gate is absolute: routing does not flip to manifest-driven before this pipeline exists — a fetched-but-unsigned interim would be strictly worse than the shipped constants it replaces.

## The five decisions

### 1. Task classification: keep the four tiers; add a capability floor, not more tiers

The temptation is to replace `deep/standard/cheap/review` with something richer — the five-dimension rubric (risk, complexity, uncertainty, context load, tool intensity) from the token-efficiency research, or free-form requirements, or letting the orchestrator name models outright. The last is rejected permanently and for the founding reason: an LLM's model knowledge froze at training (SPEC §6). The dimension vector was seriously considered and loses on a subtler point: the research doc's own rubric collapses the five dimensions to a tier by a max rule *before* routing — the dimensions are how the orchestrator should *think*, not what the machine needs to *receive*. Passing them through the spawn API adds five judgment surfaces to misjudge, five fields to validate, and zero routing power the tier doesn't already carry. More tiers (six? eight?) fail the same way: every added tier is a new classification boundary to get wrong, and the 2026-07-10 burn analysis showed the failure was never "four tiers is too few" — it was the values behind the tiers.

What the four tiers genuinely cannot express is the capability floor. `cheap` conflates "small coding task" with "non-coding chore," and only the latter is safe on a model beneath coding capability. So the spawn request grows one small facet, not a taxonomy: **`kind: coding | review | research | mechanical`**, orchestrator-supplied, defaulting by tier (`review` tier → review, everything else → coding — the conservative default, because over-flooring wastes money and under-flooring wastes work, and only one of those gets corrected). The router turns `kind` into an eligibility predicate over the manifest's per-model flag: a `coding` task requires `codingCapable`; `mechanical` and `research` accept any listed model. That is the entire capability-awareness mechanism — a filter, two enum fields, no scoring. Tier stays the *cost* judgment; kind becomes the *floor* judgment; the two are orthogonal and both are things an LLM classifies well without knowing any model's name.

Cost: one more field the orchestrator can get wrong, and a `mechanical` misclassification routes real coding to a bottom-of-list model exactly as today. Telemetry (escalation rate by kind) is the check, and the default-to-coding rule bounds the blast radius to tasks explicitly marked down.

### 2. Where model knowledge lives: discovery ∩ manifest, with a fallback ladder that never quietly guesses

The alternatives considered for where knowledge lives: keep compiling it into constants (status quo — fails the release-independence requirement by definition); trust discovery alone (fails because discovery has no judgment: it can say a model exists and lacks effort support, not that it shouldn't write code); trust the manifest alone (fails because the manifest can't know an *account* — entitlement, hidden flags, and alias resolution are per-account facts only discovery has). The composition is the answer, and its shape is now literally an intersection: derivation picks from `manifest list ∩ discovered(entitled, non-hidden, fresh)`. A model absent from every manifest list is *structurally* unroutable by Hive's own choice — the unknown-model rule ("never auto-route what nobody vetted") stops being a rule to enforce and becomes a property of the data shape. Guessing a new model upward is how a coding task lands on something that can't do it; guessing downward silently wastes frontier quota; the manifest-gap cost is real and named — when a vendor ships a new lineup, auto-routing cannot use it until a manifest refresh arrives, and users who want it sooner pin it in `routing.toml`, the escape hatch working exactly as designed.

When the intersection is empty — no manifest, no discovery, or nothing survives — the first draft fell straight back to today's shipped `DEFAULT_ROUTING`, and review caught the contradiction: the shipped table names models no record vouches for, which is exactly the "unknown, auto-routed" case the intersection exists to prevent. The fallback is therefore a **ladder, most-recently-verified first**: (1) the last-known-good derived snapshot — the most recent non-empty intersection, cached with its timestamps, used past its TTL only here and labeled stale; (2) the **provider's own default** — launch with no model flag at all, which runs whatever the account is entitled to by construction (this is today's Codex `"default"` posture, extended to Claude, whose no-flag launch uses the account's `default` menu entry); like today's `"default"`, it yields an incomplete execution identity and inherits that path's existing restrictions; (3) the shipped concrete table, as a **loud compatibility exception** — one deduplicated warning naming which rung failed and why, because a router silently running on compiled-in guesses is the status quo this design exists to end, acceptable only as the floor under a fresh install that has never once discovered anything.

Concrete retirements this funds, each today a release-coupling: `CLAUDE_BEST_MODEL` — retired by obsolescence, not by discovery, and the distinction matters: the menu *omits* `best` entirely (its `default` entry resolves to Opus 4.8), so no vendor surface can confirm what `best` launches and the constant is a cache of a fact only a billable probe establishes. Derived routes select from manifest lists of canonical ids and never emit an unlisted alias, so nothing load-bearing needs the constant; it survives only as a deprecated shim for user-pinned `best`, which `docs/model-selection.md` already warns against pinning. Likewise `modelVendor`'s alias list (names join against discovered catalogs; the regexes remain only as a last-resort parse for names discovery has never seen); the direct file-reads in `resolveConcreteModel` for Codex `"default"` (`~/.codex/config.toml` misses layered/profile config — the app-server's `config/read` answers with the *effective* unflagged default); and `FABLE_AUTO_ROUTING_CUTOFF` with `POST_FABLE_CUTOFF_DEEP_MODEL` (a billing-date route change is manifest data if anything is — the next such cutoff should be an edit to the fetched candidate lists, not a code change with a cross-verification test).

### 3. Effort: a first-class, discovery-validated knob, plumbed end to end or not at all

Effort becomes a peer of `model` at every layer it exists today for Codex and is absent for Claude: `ClaudeRouteSchema` grows an optional `effort`; `SpawnRequest` grows an optional `effort` (a directive, like `model`: passed only when the caller means it; routine spawns take the resolution ladder in decision 4); the Claude adapter passes `--effort <level>` (verified present, claude 2.1.207); and the Claude arm of `ExecutionIdentity` grows an effort field, because a knob that spawn honors and control-restart drops is precisely the silent divergence the immutable identity exists to prevent (the token-efficiency doc's R3 called this out, and it is right).

Three design points the plumbing has to get right:

**Effort values are vendor data, not a Hive enum.** Hive's schemas currently hardcode the levels as closed enums — `minimal|low|medium|high|xhigh` in `CodexRouteSchema` and the Codex identity arm (`src/schemas/routing.ts:22-28`, `src/schemas/agent.ts:36`) — and the live catalogs show those enums are *already* wrong in both directions: no listed Codex model advertises Hive's `minimal`, while Sol and Terra advertise `max` and `ultra` the schema would reject. A closed enum here is `CLAUDE_BEST_MODEL` in another costume — a shipped constant caching a fact the vendor publishes — and it fails worse, because a schema rejection doesn't misroute a spawn, it refuses a valid one. So effort becomes an open string at every layer (route, spawn request, identity), sanity-bounded at ingestion (length-capped, `[a-z0-9-]` charset — it becomes argv) and validated at spawn time against the resolved model's discovered `effortLevels`, with the known-value list serving only as fallback validation when no record exists. A normalized Hive-only scale mapped per vendor was also considered and rejected: a lossy mapping means the user asks for one thing and gets another — the exact lie the verbatim-model rule exists to prevent.

**Discovery gates the flag, and provenance decides what failure means.** Haiku is the proof case: its menu entry advertises no effort support, so a per-tier effort default naively applied would pass `--effort low` to a model that may not take it. Rule: a resolved candidate whose record does not *affirmatively* advertise effort support gets no effort flag. When validation fails, what happens keys on the effort's *provenance*, which the resolution ladder carries with the value — the first draft keyed it on which config object the value arrived in, and review caught the hole: a `routing.toml` effort pin is user-authored, but it arrives via "the route table," so it would have been clamped as if it were Hive's own judgment. Restated on provenance: **user-authored effort** (a spawn directive *or* a `routing.toml` pin) that a *discovered* model's record doesn't list *fails the spawn loudly* with the record as evidence — the record is positive evidence and the user should know their ask can't run. User-authored effort on an *undiscovered* model (a pinned brand-new id) passes through verbatim with a warning: absence is unknown, not refusal, and the CLI's own launch failure is the honest validator. **Hive-authored effort** (a manifest tier default, the model's discovered default, the shipped constant) clamps with the decision `reason` noting it — clamping Hive's own defaults is housekeeping; clamping a human's directive is disobedience, whichever file the human wrote it in. The same gate fixes a quieter shipped constant: several spawner and recovery paths default a missing Codex effort to `medium` (`spawner-impl.ts:1086,1238,1288`), but the catalog publishes a per-model `defaultReasoningEffort` (Spark's is `high`, not `medium`) — an unset effort falls back to the model's own discovered default, and to the shipped constant only beneath that.

**The identity records what was effective, not what was flagged.** The first draft let a Claude identity omit effort, reading absence as "the CLI default, which is what launched — faithful." Review broke that: CLI defaults *drift* — the same absent flag replayed after a CLI upgrade launches a different effective effort, which is precisely the launch-drift the immutable identity exists to prevent. So every new identity whose resolved model supports effort persists the **effective** effort — the resolution ladder's outcome, which always terminates (worst case at the shipped constant) — even when no flag would have been passed; the restart then pins it explicitly. A model that doesn't support effort records none, and that identity is complete. Legacy rows without the field are what they are: non-reproducible on this axis, restarted without a flag as today and labeled as compatibility rather than fidelity — retroactively inventing what their launch's effective default was would be an estimate wearing a measurement's badge.

### 4. Deriving defaults: resolve tier → route through the manifest at spawn time, with routing.toml untouched on top

The resolution order is three layers, strictly ranked: **user pin** (`routing.toml`, format unchanged — per-tier, per-tool, always wins) → **derived route** (the manifest list ∩ discovery intersection of decision 2) → **the fallback ladder** (decision 2, rung by rung). The tier stays the key; what a tier *means* moves from "these strings we shipped" to "this candidate list resolved against what your account can actually reach, right now."

Three points the first draft left implicit, each caught by review, each needing to be explicit because getting them wrong is silent:

**Model resolves first; effort is a function of the resolved model, never a parallel column.** Because `routing.toml` merges per-field within a tier×tool cell (`src/config/load.ts:96-102`), a naive per-field layer stack would let a user's pinned model pick up a derived effort computed for a *different* model. The fix is ordering, not merging: resolve the model through the three layers, then resolve effort *against that model* — spawn-level directive first, then the cell's pinned effort, then the manifest's per-tier effort default (the knob that makes a cheap tier actually reason cheaply), then the resolved model's own discovered `defaultReasoningEffort`, then the shipped constant — with each step validated against the resolved model's record and its provenance carried (decision 3). No cross-model value can survive that ordering.

**A pin is a standing user directive, and it interacts with the capability floor the way explicit choices always do in Hive: never silently overridden, never silently obeyed.** A `routing.toml` model pin counts as "explicit" for the unroutable-unknown rule (that rule would be vacuous otherwise — the table is where users pin things), and a pinned model that fails the task's `kind` floor spawns anyway, with the conflict named loudly in the decision reason and the spawn report — the user's standing judgment about their own account outranks the manifest's, exactly as a quarantined route is still used when the user pinned it (SPEC §6). Failing every coding spawn on a tier because of a table entry would convert a judgment disagreement into a work stoppage; hiding the disagreement would convert it into a silent misroute. One tightening for pins of models Hive cannot classify: a spawn-level `model` whose vendor neither discovery nor the name heuristics can establish now *requires* an explicit `tool` and otherwise refuses with guidance — today it silently stays on the tier's routed tool (`spawner-impl.ts:1014`), which is a coin-flip that happens to be labeled a default. Table pins never have this problem: the cell (`[deep.claude]`) names the tool by construction.

**A stale record that lies gets one bounded retry, and the transaction is specified.** The first draft claimed a stale-record launch failure would "naturally" fall back via the readiness monitor; review correctly flagged that as not current behavior — `monitorReadiness` quarantines the route (SPEC §6's viability machinery) and reports; nothing respawns. The design makes the retry explicit and minimal: when a *Hive-derived* candidate fails to launch, the failed attempt settles its reservation and records against the route exactly as today, and the spawn request then re-enters resolution **once** — the quarantine it just created excludes the failed candidate, so the intersection yields the next list survivor or the ladder's next rung — under a fresh reservation. One automatic retry, never more, and never for user-pinned models, whose failures report with the capacity-and-fallback message rather than being second-guessed (substituting a user's named model is a lie regardless of why it failed).

The auditability worry about per-spawn derivation is real, and gets a direct answer rather than a materialized table: a `hive routing` surface that prints the currently-derived table with per-cell provenance — pinned / derived (with manifest version and record timestamps) / which fallback rung — because a router whose choices can't be inspected will be distrusted the first time it surprises someone, and *should* be.

### 5. Capability is eligibility; quota is ranking; identity is immutable — three stages that never trade places

The quota resolver is already generic over an arbitrary candidate list, scored by headroom, filtered by viability, gated per-pool (SPEC §6). This design does not touch its internals; it changes who builds the candidate list, and what a candidate *is*. The hardcoded Fable→Opus splice generalizes into the manifest's own ordering: after the primary candidate, the remainder of the tier's list — intersected with discovery like the primary — *is* the downshift chain, and the end of the list is the floor no headroom number may cross. This reproduces today's valve (deep's Claude list is `[claude-fable-5, claude-opus-4-8]` before the cutoff manifest edit, `[claude-opus-4-8]` after) as data instead of code. Order semantics are preserved exactly: primary first, ties keep the primary, so the no-quota-data compatibility case still behaves as today. And a candidate is now a **triple** — `{tool, model, effort}`, the effort resolved and validated per candidate before scoring — because the first draft's pairs left effort to be read off the primary route *after* the resolver picked, so a chain candidate would have launched with an effort chosen for a different model, and the reservation ledger would never learn that an `xhigh` run and a `low` run are different quantities. Making estimates actually condition on effort is deferred to telemetry (inventing effort multipliers is the accurate-or-unknown rule's territory), but the identity of what was reserved must be honest now, or there will be nothing truthful to calibrate against later.

The load-bearing rule is the stage ordering. **The capability floor filters candidates before quota ever sees them**: a Hive-chosen model beneath the task's `kind` floor never enters the list (user pins are the one exception, entering with the loud-compliance rule of decision 4), so no amount of quota pressure can downshift a coding task below the floor — exhaustion of every eligible candidate fails the spawn with the capacity report, exactly as an exhausted explicit pin does today. The rejected alternative was making capability a scoring penalty rather than a filter — tempting because it "uses idle cheap capacity" — and it is rejected because a penalty is a price, and under enough quota pressure any price gets paid; a floor that can be outbid is not a floor. The same argument already lost once inside quota design ("headroom alone ranked a route that could not run" — SPEC §6's viability lesson); capability is the same shape of fact.

One drift mode connects this decision to the quota subsystem and was invisible until cross-vendor review lined the two up. Quota's model-scoped pools bind by joining the pool's provider-given display name against the provider's catalog, and that join "fails safely to unbound" on a rename (SPEC §6) — but capability records join the same catalog on their own schedule, so a renamed or newly-shipped model can become *auto-selectable* (fresh discovery, manifest already lists it) at the exact moment its own meter is *unbound* (stale name on the quota surface). Safe-fail on one side plus eager-adopt on the other composes into the unsafe whole: the router's newest choice is precisely the one whose per-model cap has stopped gating, and the general pool's headroom waves it through — the 2026-07-11 Fable-at-99% incident with a fresh coat of paint. Two rules close it. First, **one join, one code path**: capability records and pool bindings resolve names through the same catalog snapshot, so they cannot disagree about what a name means — they can only both know or both not. Second, **an unbound model-scoped pool quarantines auto-adoption on its provider**: while any model-scoped pool on a provider binds to nothing in the catalog, models on that provider that lack a bound meter of their own are not auto-routable — explicit pin only, warning loudly — because the orphaned pool may be exactly their meter under its old or new name. The quarantine lifts the moment the join heals (next catalog or quota refresh), and it is deliberately provider-scoped rather than global: a Codex rename should not stop Claude routing.

Execution identity and control restarts constrain all of this from below, and the constraint is welcome: **manifest and records are consulted at launch time only, and never at restart**. The identity — `{tool, model, effort}` with effort effective-at-launch (decision 3) — remains the frozen, complete instruction for reproducing the launch; a critical-control restart replays it verbatim even if the manifest has since delisted or the catalog renamed the model, because a faithful restart of the interrupted agent is the point and the recorded-model-no-longer-launchable case already fails closed with a durable alert (SPEC §6). Nothing dynamic may be needed to restart an agent: this is why effort lives *in* the identity rather than being re-derived, why derived routes must resolve to concrete launch tokens *before* the identity is written (they do — the resolver's output is concrete by construction), and why the `live_model` observation stays a separate field rather than mutating `model`. The dynamic router makes launch-time choice smarter; it must leave the restart path exactly as dumb as it is.

## Migration from today's tiers

The four tier names, the `routing.toml` format, and every existing pin remain valid throughout — there is no user-visible migration, only the defaults beneath the overrides getting smarter. Staged, each step inert until the next:

1. **Record, change nothing.** Land the capability-record store and its discovery feeds (Claude's `initialize` is already awaited; Codex's `model/list` rides the app-server handshake), provenance-stamped, visible in `hive routing` — observation only, routing still reads the shipped table.
2. **Plumb effort inert.** Schema fields (`ClaudeRouteSchema.effort`, `SpawnRequest.effort`, identity effort as open strings), adapter flag, discovery-gated validation — all defaulting to unset, so shipped behavior is bit-identical until a user sets a value.
3. **Stand up the manifest pipeline.** Signing, expiry, atomic install, rollback, kill switch, audit (the trust section above) — plus the first manifest, whose candidate lists byte-for-byte reproduce today's shipped table so that adopting the mechanism changes no route. This step is a hard prerequisite of step 5, not an optional hardening after it.
4. **Shadow-derive.** Compute the derived route per spawn, log derived-vs-shipped divergence, route on shipped. The flip criteria are concrete, not vibes: over the shadow window, zero `kind`-floor violations by the derived choice; zero derived candidates that discovery later contradicted (launch or entitlement failures had they run); every divergent cell attributable to a discovered or manifest fact rather than a resolution bug; and projected planning-unit cost for the same task mix no worse than shipped. Any criterion failing restarts the window after the fix.
5. **Flip the middle layer.** Derived routes govern between pins and the fallback ladder; the manifest's list remainders replace the hardcoded valve; the `kind` floor gates candidates. Reversible per-tier by a `routing.toml` pin, globally by the manifest kill switch; the post-flip rollback trigger is equally concrete — launch-failure or escalation rate rising above the pre-flip baseline reverts first and investigates second.
6. **Retire the constants.** `CLAUDE_BEST_MODEL`, the cutoff pair, the alias list in `modelVendor` — each deleted only once its manifest/discovery replacement has carried a real lineup change (the 2026-07-12 Fable cutoff is, conveniently, an immediate live test: encode it as a manifest list edit in step 3 and watch it govern).

## Open questions

- **How stable are the discovery surfaces as contracts?** The two vendors sit at different epistemic tiers here: Codex's `model/list` is officially documented (OpenAI's app-server docs direct clients to call it before rendering model selectors — see `router-vendor-surfaces.md`), while Claude's `initialize` model menu is undocumented, the same tier as `get_usage`. Capability records carry per-record provenance and CLI version for exactly this reason, but the design has no answer yet for a surface that *silently* drops a field rather than disappearing.
- **Who operates the manifest pipeline, and how fast is fast enough?** The trust section specifies the mechanism; it cannot conjure the operational commitment. "Hours-to-days, not a release" assumes someone publishes on that cadence; if manifest refresh in practice rides releases, the release-independence claim quietly dies. The structural unroutability of unlisted models makes the failure visible (new models are pinnable but not auto-routed) rather than dangerous, but visible-and-slow is still a broken promise.
- **Is `kind` defaulting to `coding` too conservative?** It routes ambiguous cheap chores onto coding-capable models, spending real money to avoid misrouting. Escalation-rate and cost telemetry by kind decides; the reverse default is indefensible without it.
- **Effort defaults per tier are judgment with no data.** The manifest will ship some (`deep: high`, `cheap: low`, presumably) — but the token-efficiency doc's open question stands: nothing yet measures whether a given effort step pays for itself, and the A/B machinery it proposes should run before effort defaults get opinionated.

## Verified facts vs. assumptions

**Verified live on this machine, 2026-07-11, claude 2.1.207:** the `initialize` control request returns `models[]` with `value`, `resolvedModel`, `displayName`, `supportsEffort`, `supportedEffortLevels`, `supportsAdaptiveThinking`, and an `account` block with `subscriptionType`; this account's menu and resolutions as quoted above; Haiku's entry omitting the effort fields; `--effort <level>` present in `claude --help`. **Verified in this repo's code today** (cited inline): every `src/` claim above, including that readiness failure quarantines without respawning and that an unrecognized spawn model silently stays on the routed tool. **Verified live by caleb, 2026-07-11, codex 0.144.1 and claude 2.1.207** (`docs/research/router-vendor-surfaces.md`): `model/list` is zero-cost, officially documented, and returns account-visible models with per-model default and supported reasoning efforts, hidden/default flags, input modalities, and service tiers; `config/read` reports the effective unflagged default; the live catalogs include effort values (`max`, `ultra`) outside Hive's current schema enums and omit `minimal` everywhere; and Claude's menu omits `best`, so the `CLAUDE_BEST_MODEL` binding is not vendor-surface-provable. **Taken from this repo's dated docs, verified 2026-07-10 against live CLIs, not re-verified today:** the Codex config-only effort flag mechanics; the entitlement-failure behavior of concrete Codex models; both vendors' quota surfaces. **Assumptions:** that a manifest publishing path with the trust properties above will exist and be operated; that the discovery surfaces are stable enough across CLI versions to treat as contracts — Codex's `model/list` is officially documented, but Claude's `initialize` menu is not (the same epistemic tier as `get_usage`, which Hive labels `reported`, and its capability records should carry the same humility).
